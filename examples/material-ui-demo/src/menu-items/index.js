import dashboard from './dashboard';
import nodes from './nodes';
import edges from './edges';
import layout from './layout';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, nodes, edges, layout]
};

export default menuItems;

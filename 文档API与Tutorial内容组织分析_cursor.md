# LogicFlow 文档内容组织分析（`sites/docs/docs/api` + `tutorial`）

本文基于对官方文档站点中 **API** 与 **教程（指南）** 目录下全部中英文 Markdown 的通读与交叉核对，从**内容顺序、叙事逻辑、学习路径、主题聚合度、与导航配置的一致性**等维度归纳问题，并给出可落地的优化建议。分析日期：2026-04-22。

---

## 1. 分析范围与信息架构现状

### 1.1 目录规模

| 区域 | 规模（约） | 说明 |
|------|------------|------|
| `docs/api` | 26 篇（中/英成对） | 实例 API 索引、详情页、事件、主题、Model、类型字典等 |
| `docs/tutorial` | 76 篇（中/英成对） | 介绍、基础、进阶、插件等指南 |

### 1.2 站点侧导航（`.dumirc.ts`）

- 教程侧 slug：`tutorial/basic`（order 3）、`tutorial/advanced`（order 4）、`tutorial/extension`（order 5）。
- 文档内 `group.order`：介绍 `0` → 基础 `1` → 进阶 `2` → 插件 `3`，与上述 slug 顺序整体一致。
- **缺口**：`about`、`get-started`、`update` 等挂在「介绍」分组，但 `.dumirc.ts` 未为「介绍」单独配置 slug，依赖 dumi 默认聚合；长期维护时容易出现「首页入口」与「侧栏分组」心智不一致。

---

## 2. 学习路径与内容顺序

### 2.1 优点

- **由浅入深的大框架清晰**：介绍 → 基础（实例、节点、边、主题、网格、背景、事件）→ 进阶（自定义节点/边、框架集成、编辑体验）→ 插件，符合多数用户认知。
- **快速上手**覆盖安装、最小示例、数据格式与插件引入，作为第二篇（order 1）合理。

### 2.2 问题

1. **「实例」与「快速上手」的认知负担**  
   - 基础分组第一篇标题为「实例」，侧重 `LogicFlow` 实例概念与创建；「快速上手」反而在介绍组且已包含创建与渲染。新用户可能疑惑：应先读「快速上手」还是「基础-实例」。  
   - 建议在介绍或快速上手中用一段话明确**推荐阅读顺序**（例如：先快速上手跑通 → 再回读基础篇补全概念）。

2. **「边」主题拆成两篇且标题区分度不足**  
   - `tutorial/basic/edge`：内置边类型 + 继承式自定义边。  
   - `tutorial/advanced/edge`：React 自定义边、锚点数据、动画等。  
   - 进阶篇标题仅为「边」，侧栏中与基础篇「边 Edge」并列时，**不易一眼看出进阶篇独有内容**。  
   - 建议进阶篇标题改为能体现差异的命名（例如「边：React 与锚点、动画」），或在基础边文末增加「下一步」指向进阶篇。

3. **升级文档在「介绍」组末尾**  
   - `update`（升级到 2.0）与「是什么」「快速上手」同属介绍，**对新用户是噪音**，对老用户是刚需。  
   - 建议单独分组或子路径（如「迁移 / 从 1.x 升级」），或放入更新日志站点区域并在快速上手中链过去。

4. **事件在基础组顺序偏后（order 6）**  
   - 事件是串联交互的主线，很多基础能力（节点、边、插件）都会引用事件。  
   - 当前顺序：实例 → 节点 → 边 → 主题 → 网格 → 背景 → 事件。若目标用户以「先能响应交互」为主，可将事件略提前（例如在边之后、或与节点交错），并在各篇减少重复解释 `lf.on`。

---

## 3. API 文档：结构、重复与入口

### 3.1 三层 API 信息并存

当前存在：

- **`api/index.zh.md`**：按能力域分表的「实例方法索引」（大量锚链到 `detail/index`）。
- **`api/detail/index.zh.md`**：**超长**的方法级说明（同一份内容承担「参考手册」角色）。
- **`api/detail/constructor.zh.md`**：构造参数与部分内嵌类型说明。

**问题**：同一类知识（例如 `setTheme`、画布变换、数据读写）在索引表、详情长文、构造参数表之间**多次出现**，维护成本高，且用户不确定应以哪一篇为「权威定义」。

**建议**：

- 明确分层：**索引页只保留签名级摘要 + 链到唯一详解**；详解集中在 `detail/index` 或按域拆分为多文件（Graph / Node / Edge / Transform…）。  
- 构造参数以 `constructor` 为唯一详解，`index` 表仅链到该页对应小节。

### 3.2 `api/detail/index.zh.md` 缺少统一 frontmatter

该文件开头直接进入正文，与同目录其他页的 `nav` / `title` / `order` 不一致，可能导致侧栏标题、搜索摘要或主题行为与其他页不统一。

**建议**：补全与其他 API 子页一致的 meta，并视长度**拆文件**（否则单文件两千行以上不利于检索与 Git 协作）。

### 3.3 类型文档命名与语义

- **`api/type/MainTypes.zh.md` 标题为「实例」**，正文实为「LogicFlow 各配置项的类型定义」「Common（实例基础配置项）」等。  
- 与教程「基础 - 实例」（`LogicFlow` 实例对象）**同名不同义**，极易误导。

**建议**：将类型篇标题改为「初始化与画布类型」「Options 与数据类型」等，与教程中的「实例」区分。

### 3.4 API 侧栏 `order` 重复（`.dumirc.ts`）

`api/model` 与 `api/type` 的 `order` 均为 `6`，在依赖 order 排序时可能出现**顺序不稳定或依赖文件名**。建议为每个侧栏分组分配唯一 order。

### 3.5 主题相关三处入口

- `api/theme.zh.md`（`setTheme` 等用法侧写）  
- `api/type/Theme.zh.md`（类型）  
- `tutorial/basic/theme.zh.md`（指南）  

**问题**：主题作为横切关注点，三处各有侧重但未在任一处做**总览导航**（「概念 → 教程示例 → API → 类型」一条链）。

**建议**：在教程主题篇首增加「相关 API / 类型」模块；在 `api/theme` 或类型导览中回链教程。

---

## 4. 插件文档：主题分散与聚合

### 4.1 现状

- `extension/intro` 承担插件机制说明（`LogicFlow.use`、实例级 `plugins` 等），其后大量单插件文档平铺。

### 4.2 问题

1. **平铺过长**：泳道、BPMN、分组、动态分组、布局、适配器等**场景差异大**，同一层级展示会使用户难以建立「我该看哪几篇」的路径。  
2. **同组内 `order` 冲突**：例如中文插件文档中 **「圆角折线」与「高亮」均为 `order: 12`**，侧栏排序可能不符合预期或与英文不一致。  
3. **与「进阶」边界模糊**：如「渐进连线」「边上插入节点」等介于核心行为与扩展之间，若只在插件下列出，**新用户不知道属于扩展能力**。

**建议**：

- 在插件分组增加**一页「插件地图」**：按「交互增强 / 导入导出与数据 / 领域方案（BPMN、Pool）/ 布局」分类，每类链到具体文档。  
- 修正同组内重复的 `order` 数值。  
- 对强依赖业务场景的插件，在进阶相关篇（如边、节点）末增加「可选插件」链接。

---

## 5. 交叉引用、版本叙事与一致性

### 5.1 已发现的链接问题（组织与维护类）

- `tutorial/basic/event.zh.md` 中 `eventCenter` 链为 `../../api/graphModel.zh.md`，实际路径应为 **`../../api/model/graphModel.zh.md`**，属于**跨目录引用未随文件结构调整更新**的典型问题。

### 5.2 代码块标签笔误（影响「可信度」）

- `api/detail/constructor.zh.md` 中存在 `` ```tsx | purex | pure `` 一类 fence 标记错误，易造成渲染或复制体验问题；与「文档组织」同属**质量基线**，建议一并修正。

### 5.3 1.x / 2.x 与 CDN 叙事分散

- 「快速上手」「插件简介」等处同时出现 1.x 与 2.x 的 CDN、路径差异。  
- **问题**：版本差异未集中在一页「版本与迁移」中，用户容易**复制错误时代的片段**。

**建议**：维护单一「版本与包引入」短页，其他篇只保留 2.x 默认示例 + 「若使用 1.x 见链接」。

---

## 6. 教程与 API 的互补关系

### 6.1 做得好的地方

- `eventCenter.zh.md` 与 `tutorial/basic/event.zh.md` **互为指向**，符合「教程讲用法、API 列全量」的模式。

### 6.2 可加强之处

- **Model 系列**（`graphModel` / `nodeModel` / `edgeModel`）对初学者门槛高，教程「进阶-节点」等篇虽已提及，但缺少一篇**「何时读 Model、读哪一层」**的短引导（可放在进阶首篇或 API model 导览）。  
- **editConfigModel** 与教程「图编辑配置项（silent-mode）」关系紧密，建议在教程篇显式链到 API，减少「配置在教程改了名、在 API 另一套术语」的割裂感。

---

## 7. 优化建议汇总（按优先级）

### 7.1 高优先级（投入小、收益大）

| 项 | 动作 |
|----|------|
| 修复错误链接 | 修正事件教程中 `graphModel` 路径；定期脚本检查站内相对链接。 |
| 消除侧栏 order 冲突 | 插件组内、`api` 分组在 `.dumirc` 与 frontmatter 中**唯一化 order**。 |
| 标题消歧 | `MainTypes` 篇标题与教程「实例」区分；进阶「边」标题体现与基础边的差异。 |
| 统一 meta | 为 `api/detail/index.zh.md` 补全 frontmatter；修正明显 fence 笔误。 |

### 7.2 中优先级（信息架构）

| 项 | 动作 |
|----|------|
| API 三层去重 | 定「单一事实来源」：索引薄、constructor 管初始化、detail 按域拆分或保留但减重复表。 |
| 插件地图 | 新增一页分类导航 + 每类推荐阅读顺序。 |
| 迁移与版本 | 「升级到 2.0」独立分组或并入 release 区；各教程默认 2.x 示例。 |

### 7.3 低优先级（体验增强）

| 项 | 动作 |
|----|------|
| 主题 / 事件横切页 | 在教程关键页增加「API ↔ 类型 ↔ 教程」三方链接块。 |
| 学习路径显性化 | 在 `about` 或 `get-started` 增加「推荐阅读顺序」示意（列表或简单流程图）。 |
| 介绍组 slug | 在 dumi 配置中为「介绍」显式配置 slug，与 basic/advanced/extension 对齐。 |

---

## 8. 结论

当前 **教程** 在「介绍 → 基础 → 进阶 → 插件」上整体合理，主要问题集中在：**同名主题多义（实例、边）、插件与 API 平铺过长、索引与详解重复维护、少量链接与 order 细节破坏导航可信度**。**API** 侧核心矛盾是 **`index` / `detail/index` / `constructor` / 类型字典** 职责重叠与 **`detail/index` 体量过大**。

按上表分阶段收敛「单一事实来源」、修正导航与链接、用一页「插件地图」和「版本引入」做总览，可在不大改产品的前提下明显提升**学习路径清晰度**与**长期可维护性**。

# LogicFlow 文档改版执行方案

> 目标：把前面几份分析收敛成一份可落地的执行方案。
>
> 这份方案同时服务两个目标：
> - **对人**：让第一次接触 LogicFlow 的用户更容易理解，学习曲线更平滑
> - **对 AI**：让文档更适合检索、总结、引用和生成代码

---

## 一、执行原则

在真正开始改文档之前，先明确这次改版遵循的原则。

### 1. 先修“入口和职责”，再补“结构和数量”

当前文档的主要问题不只是“少了几篇文档”，而是：

- 入口页承担过多内容
- 标题和正文职责不一致
- 关键桥梁概念出现过晚
- 教程、概念、参考之间缺少导航关系

因此第一阶段不建议直接大规模拆目录，而是优先修复：

- 页面职责
- 阅读入口
- 前置与下一步导航
- AI 易误解的混合页

### 2. 让每篇文档回答一个明确问题

后续改版时，每篇文档都尽量回答一个具体问题，例如：

- 我如何 5 分钟跑通 LogicFlow？
- LogicFlow 的图数据是什么？
- 我什么时候需要自定义节点？
- 如何给节点加连接规则？

避免一篇文档同时承担“概念介绍 + 高级实现 + API 参考”三种职责。

### 3. 面向 AI 的文档块要尽量自包含

每个关键页面应尽量具备：

- 标题能准确反映正文职责
- 开头说明本页回答什么问题
- 标明前置知识
- 给出最小示例
- 给出相关 API 和下一步阅读

这样 AI 即使只命中某一页或某一段，也更不容易答偏。

### 4. 实施过程中同步修正文档错误

这次改版不应只做“组织优化”，还需要把实施过程中已经发现的文档错误一并修正。

这里的“错误”包括但不限于：

- 错误链接
- 错误路径
- 失效引用
- 错误示例代码
- 标题与正文不匹配
- 版本说明混乱
- API 名称、参数名、类型名拼写错误

原则上，**凡是在改某篇文档时已经确认存在的问题，都应尽量在同一轮中顺手修复**，避免只调整结构、不修内容正确性，导致新组织下仍然保留旧错误。

### 5. 验收阶段必须结合源码做正确性校验

文档改版完成后的验收，不能只看“结构是不是更清晰”，还需要回到 LogicFlow 源代码做正确性校验。

重点要核对：

- 文档中提到的 API 是否真实存在
- 方法名、参数名、返回值与源码是否一致
- 推荐方案是否仍然是当前实现中的主路径
- React/Vue/插件/Adapter 等示例是否与当前代码结构一致
- 文档中的能力边界、默认行为、版本说明是否与源码一致

也就是说，验收标准要同时包括：

1. **组织正确**：学习路径更顺、导航更清晰
2. **内容正确**：与当前代码实现一致

---

## 二、总体改版策略

本次改版建议分三批推进：

1. **第一批：修入口质量**
2. **第二批：补桥梁与知识连接**
3. **第三批：做结构化重组**

这样安排的原因是：

- 第一批改动小，但收益最大
- 第二批开始真正降低学习断层
- 第三批再做目录和结构重组，风险更低

---

## 三、第一批：修入口质量

## 目标

让新用户第一次进入文档时，不会立刻掉进复杂概念和实现细节。

## 核心问题

当前最影响体验的入口问题有：

- `get-started` 内容过重
- `basic/class` 标题与内容不匹配
- `basic/node` / `basic/edge` 标题像基础介绍，正文却快速进入自定义实现
- 主学习路径默认把用户推入高级内容

## 建议动作

### 1. 收缩 `tutorial/get-started.zh.md`

把它收束成真正的“快速上手”页，只保留：

- 安装
- 最小初始化
- 最小 `render`
- 一个能立刻跑通的例子
- 一小段“接下来读什么”

建议从这页移出或弱化：

- adapter 详细内容
- 过多框架并列示例
- 过重的数据结构解释
- 1.x / 2.x 并排展开

### 2. 调整 `tutorial/basic/class.zh.md` 的职责表达

这页当前前半像实例介绍，后半像图数据入门。

建议第一阶段先不一定改路径，但至少要：

- 在开头明确它的真实作用：`LogicFlow 实例 + graphData 基础`
- 弱化“实例”这一过于抽象的标题影响
- 在页首加一句：如果你只想快速跑通，请先看 `get-started`

### 3. 重命名或重定位 `basic/node` / `basic/edge`

这两页的核心问题是标题误导。

建议第一阶段先做轻量修正：

- `basic/node` 明确成“自定义节点基础”
- `basic/edge` 明确成“自定义边基础”

即使暂时不改文件路径，也建议在页首增加职责说明，避免读者误以为这是“节点/边概念总览”。

### 4. 在首页或文档入口补“推荐阅读顺序”

建议在 `about`、`get-started` 或 docs 入口处补一个简单的阅读顺序：

1. 快速上手
2. 图数据与实例
3. 主题 / 事件 / 基础配置
4. 自定义节点 / 边
5. 进阶与插件
6. API 查询

## 第一批完成标准

完成后应达到：

- 新用户能在 5-10 分钟内跑通并知道下一步读什么
- 不再因为“节点”“边”“实例”等标题产生明显预期落差
- 文档入口不再混入过多版本、插件、适配器噪音
- 本批涉及页面中已发现的明显错误链接、错误表述、错误示例应同步修复

---

## 四、第二批：补桥梁与知识连接

## 目标

让用户从“会用”自然过渡到“会改”，降低从基础到进阶的断层。

## 核心问题

当前真正的断层主要在于：

- `properties`、`model/view`、锚点、连接规则这些概念出现太晚
- `advanced/node` 主题过于混杂
- React/Vue 新旧方案关系不清
- Tutorial 与 API 缺少双向桥接

## 建议动作

### 1. 给关键页面补“桥梁段落”

建议优先补这些页：

- `basic/node`
- `basic/edge`
- `advanced/node`
- `advanced/edge`
- `advanced/react`
- `advanced/vue`

每页至少加四类信息：

- 这页适合谁读
- 前置知识
- 本页不解决什么
- 下一步阅读

### 2. 把关键概念的首次出现前移

建议优先把这些概念变成“先解释，再使用”：

- `properties`
- `model / view`
- 锚点
- 连接规则
- `graphModel`
- `eventCenter`

第一阶段未必要全部做成独立文档，也可以先以“桥梁小节”的方式出现。

### 3. 拆解 `advanced/node.zh.md`

这页目前承担的主题过多，建议至少在内容层做拆分思路：

- 节点交互与约束：连接规则、移动规则、锚点
- 框架节点渲染：HTML / React / Vue
- 更新与外部通信：节点状态同步、事件通信

如果短期不拆文件，也建议加清晰的导读，告诉用户不同场景该跳到哪一节。

### 4. 明确 React / Vue 节点方案优先级

当前旧 HTML 挂载方式与新的 registry 方式容易混淆。

建议：

- 在 `advanced/react` / `advanced/vue` 开头明确这是当前推荐方案
- 在 `advanced/node` 中把旧方式定位成“历史兼容或底层原理示例”
- 明确“如果你是 React/Vue 项目，优先读独立文档”

### 5. 建立 Tutorial ↔ API 双向桥接

建议重点补：

- Tutorial 页末尾增加“相关 API”
- API 页开头增加“如果你第一次接触该能力，先读哪篇教程”

重点优先改这些 API 页：

- `api/index`
- `api/model/nodeModel`
- `api/model/edgeModel`
- `api/model/graphModel`
- `api/theme`
- `api/eventCenter`

## 第二批完成标准

完成后应达到：

- 用户从“看懂最小示例”到“理解为什么这样做”不再陡升
- React/Vue 用户能快速知道该走哪条路径
- AI 在回答节点、边、锚点、事件相关问题时，不容易把不同层级内容混在一起
- 本批改动涉及的关键概念、API 引用、示例路径应与源码实现完成一次交叉核对

---

## 五、第三批：做结构化重组

## 目标

在前两批稳定后，再重构目录与文档树，让整套文档长期可维护。

## 核心问题

当前目录的问题是：

- 教程、概念、参考混杂
- 插件目录按文件平铺，不按目标分组
- API 首页是类目索引，不是任务索引
- 超长混合页不利于长期维护

## 建议动作

### 1. 建立更明确的文档层次

可以逐步形成三层结构：

- **入门 / 教程层**：怎么开始、怎么做
- **概念 / 指南层**：为什么这样做、什么时候这样做
- **API / 参考层**：完整参数、边界条件、方法说明

不一定一步到位引入全新目录，但长期建议形成这三类稳定边界。

### 2. 重组插件文档

插件区建议按目标建立选择地图，例如：

- 编辑体验增强
- 节点/边能力增强
- 数据导入导出
- 布局与导出
- 领域方案（如 BPMN）

第一阶段可以先在 `extension/intro` 中做“插件地图”；
长期再考虑目录分组。

### 3. 给 API 增加任务型入口

API 首页建议增加一层任务导航，例如：

- 初始化与渲染
- 数据读写
- 编辑控制
- 自定义节点 / 边
- 插件系统
- 导入导出 / 适配器
- 事件系统

这样比单纯按 Graph / Node / Edge 更符合真实使用场景。

### 4. 控制超长混合页

优先评估这些页是否应拆：

- `api/detail/index`
- `advanced/node`
- `extension/adapter`

拆分标准不是“页太长”，而是“是否同时回答了多个不同问题”。

## 第三批完成标准

完成后应达到：

- 文档目录能体现学习路径，而不是只体现能力分层
- 插件区对目标导向型用户更友好
- API 既能查，也能导
- 文档维护时更容易控制单页职责
- 重组后所有核心入口页、桥梁页、API 首页都应通过一轮基于源码的正确性复核

---

## 六、建议的优先级清单

如果只按投入产出比排序，我建议优先级如下：

### P0：立即做

1. 收缩 `get-started`
2. 给 `basic/class`、`basic/node`、`basic/edge` 补职责说明
3. 给关键页补“前置知识 / 下一步阅读”
4. 明确 React/Vue 推荐方案

### P1：第二阶段做

5. 给 Tutorial 与 API 建双向桥接
6. 补关键桥梁概念的解释层
7. 把 `advanced/node` 从“大杂烩”变成更清晰的主题页或主题段
8. 在 `extension/intro` 中加入插件地图

### P2：稳定后做

9. 重构 API 首页为任务型导航
10. 拆分超长混合页
11. 逐步重构目录结构
12. 统一版本信息呈现策略

---

## 七、这份方案和前面分析的关系

这份执行方案实际上吸收了两类结论：

- 来自前面对比文档中的 **结构性问题判断**
- 来自“面向人和 AI”分析中的 **组织原则**

因此它不是单纯的目录重构方案，也不是单纯的问题总结，而是一份折中后的实施路径：

- 不一上来推倒重来
- 先解决用户和 AI 最容易踩坑的入口问题
- 再补桥梁与导航
- 最后再做长期结构化升级
- 并且在每一轮实施中同步修复已发现错误，在最终验收中结合源码校验正确性

---

## 八、最终建议

如果你准备真正开始改 LogicFlow 文档，我建议按下面的工作方式推进：

1. **先选 3-5 篇关键入口文档做试点**
   - `get-started`
   - `basic/class`
   - `basic/node`
   - `advanced/node`
   - `extension/intro`

2. **先不追求全量重构，而是验证原则是否有效**
   - 用户是否更容易知道下一步读什么
   - AI 是否更容易从单页生成稳定回答

3. **确认试点有效后，再扩展到目录层和 API 层**

一句话总结这份执行方案：

> **先把文档变得“更会带人”，再把它变得“更像体系”，最后把它变得“更适合长期维护和 AI 使用”。**
